# Database
DATABASE_URL="your_database_connection_string_here"

# Auth — REQUIRED. Min 32 chars. The server refuses to start on a missing,
# short, or placeholder value. Generate one with:
#   node -e 'console.log(require("crypto").randomBytes(48).toString("base64"))'
JWT_SECRET=""

# Reverse-proxy hops in front of the app (0 locally; 1 on Render/Railway/Fly).
# Wrong value = rate limiting keys every request to the proxy IP.
TRUST_PROXY="0"

# Initial admin account — consumed by `pnpm --filter server run seed`.
# Public /register can only ever create a PROSPECT, so the first ADMIN is
# provisioned here.
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="Change_Me_Str0ng!"

# Cloudflare R2 — object storage for uploaded documents (transcripts, IDs…).
# Create a bucket + an API token (Object Read & Write) at dash.cloudflare.com.
# Leave blank in dev/CI: uploads then fail with a clear error, not at boot.
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET=""
